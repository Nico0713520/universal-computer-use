# Third-Party Notices

This npm package depends on the following separately distributed MIT-licensed software:

- `@modelcontextprotocol/sdk` 1.30.0 — Model Context Protocol TypeScript SDK, Anthropic, PBC.
- `@trycua/cua-driver` 0.22.1 — Cua Driver TypeScript SDK. The signed native Cua Runtime is installed separately and is not bundled in this package.
- `zod` 4.4.3 — Copyright Colin McDonnell and contributors.

The full dependency graph is fixed in `pnpm-lock.yaml` during development. Dependency license texts remain available in their respective upstream distributions. This package's own code is licensed under the MIT License in `LICENSE`.

UI-TARS Desktop and the OpenAI Agents SDK were reviewed as design references; their source code, model code and native binaries are not included in this package. Exact reviewed revisions and adoption modes are recorded in the repository's `docs/upstream-sources.md`.
